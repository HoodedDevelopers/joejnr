{
  "name": "joejnr",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "node-sass": "^4.14.1",
    "react": "^16.5.0",
    "react-dom": "^16.5.0",
    "react-scripts": "^5.0.1",
    "sass": "^1.14.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "watch": "npm run sass:compile & npm run start",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "sass:compile": "node-sass --include-path sass ./src/main.scss ./src/App.css -wo"
  }
}
